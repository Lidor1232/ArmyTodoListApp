import {onNavigateToCreateTask} from '../../../../navigation/navigation.controller';

export function onPress({navigation}: {navigation: any}): void {
  onNavigateToCreateTask({
    navigation,
  });
}
